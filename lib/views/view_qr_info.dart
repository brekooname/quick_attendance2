import 'package:flutter/material.dart';
class ViewQrInfo extends StatelessWidget {
  const ViewQrInfo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
